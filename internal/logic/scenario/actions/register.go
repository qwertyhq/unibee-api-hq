package actions

// This file ensures all action executors are registered via their init() functions.
// Import this package to register all built-in actions.

// Import side effects — each action's init() registers itself in scenario.ActionRegistry.
